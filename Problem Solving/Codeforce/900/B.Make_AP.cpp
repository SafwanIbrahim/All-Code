#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define endl '\n'

int32_t main()
{   
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    int t; cin >> t;
    while(t--) {
        // wrong ans :

        // bool yes = false;
        // int a,b,c; cin >> a >> b >> c;
        // int new_a = b-(c-b);
        // int new_b = a+((c-a)/2);
        // int new_c = b+(b-a);
        // if (new_a % a == 0 and new_a != 0) yes = true;
        // if (new_b % b == 0 and (c-a) % 2 == 0 and new_b != 0) yes = true;
        // if (new_c % c == 0 and new_c != 0) yes = true;
        // cout << (yes ? "YES\n" : "NO\n");
    }
    
    return 0;
}  