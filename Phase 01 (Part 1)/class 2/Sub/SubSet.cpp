#include <bits/stdc++.h>
using namespace std;

int main()
{
    int arr[5]={1,2,3,4,5}; // subset does't maintain order and eliments can be missing like : {1,5,3} is SS

    cout << (1 << 5); // all subset of arr = pow(2,5); // 1 can be taken or not // n can be taken or not
    // same from 1 to 5 all can be taken or not so product rule would be 2*2*2*2*2 means 2 power 5
    // set doesn't contain duplicate eliments so no need to minus anything


        



}