#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a;
    cin >> a;  // it takes input if it finds non space , then stops when it gets a not digit
    cout << a <<endl;

    char s[101];

    cin >> s;  // it takes input if it finds non space , then stops when it gets a space
    cout << s;
}