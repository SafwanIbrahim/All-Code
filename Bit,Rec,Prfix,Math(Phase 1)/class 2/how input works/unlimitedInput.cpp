#include <bits/stdc++.h>
using namespace std;

int main()
{
    int x;
    int sum = 0;

    while (cin >> x)
    {
        sum+=x;
    }
    cout << sum;

    return 0; 

    // run again after entering numbers;
}